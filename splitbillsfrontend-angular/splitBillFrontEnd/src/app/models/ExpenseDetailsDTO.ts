export default interface ExpenseDetailsDTO {
  expenseId: string;
  expenseTitle: string;
  userId: string;
  userName: string;
  spendAmount: number;
  groupId: string;
  emailId: string;
  perPerson: number;
}
